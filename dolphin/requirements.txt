# Deps for the dolphin YOLO/Neutron demo (target board).
#
# The working setup needs NO pip installs. The board's system python3 is 3.13, and the BSP ships
# gi (GStreamer/Wayland), tflite_runtime (ABI-matched to the neutron delegate), cv2 and numpy for it.
# Create the venv with --system-site-packages so it can see them:
#     python3 -m venv --system-site-packages ~/dolphin/venv
#
# The two lines below are only needed for a fully isolated venv (not recommended here: gi and a
# delegate-matched tflite_runtime are not pip-satisfiable, so the NPU path would not run).
# numpy
# opencv-python-headless
