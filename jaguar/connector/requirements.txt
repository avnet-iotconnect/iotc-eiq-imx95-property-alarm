# eIQ AAF Connector dependencies, pinned to what is verified working on the board
# (BSP LF6.18.2-1.0.0, rt-sdk-ara2 2.0.4, Ara-240 firmware 131072).
#
# optimum-ara is NOT here: it is a local wheel, installed separately. See README.md.
# Its version choice is the single most confusing part of this stack.
#
# This is the connector's own venv and it must NOT use --system-site-packages:
# torch 2.9 here would collide with the BSP tflite_runtime/gi/cv2 stack that every
# other pilot needs. Keeping them apart is the whole point of the REST boundary.

fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.12.5
pydantic-settings==2.14.2

torch==2.9.0
torchvision==0.24.0
transformers==4.57.6

outlines-core==0.2.14      # guided generation (schema-constrained tool calls)
regex==2026.2.28
jinja2==3.1.6

# vision-language models
pillow==12.1.1
opencv-python==4.13.0.92
qwen-vl-utils==0.0.14

# semantic prompt cache (off by default in server_config.json)
sentence-transformers==5.3.0

# pulled in by the connector's audio paths; unused by the models we run
soundfile==0.13.1
librosa==0.11.0
